# FairFence Application Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real credentials to version control

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Supabase Configuration (Required for database and authentication)
# Get these from your Supabase project settings -> API
# https://supabase.com/dashboard/project/YOUR_PROJECT_ID/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Server Configuration (Required)
PORT=5000
NODE_ENV=development

# Session Configuration (Required for admin authentication)
# IMPORTANT: Generate a strong random secret for production
# Use: openssl rand -base64 32
SESSION_SECRET=generate-a-strong-random-secret-here-32-chars-minimum

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# SendGrid Email Configuration (Optional - app will function without email)
# Email features (contact forms, site surveys) will be disabled if not configured
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# SENDGRID_API_KEY=SG.your-sendgrid-api-key-here
# SENDGRID_FROM=admin@fairfencecontracting.co.nz
# SENDGRID_TO=admin@fairfencecontracting.co.nz
# SENDGRID_SANDBOX=false

# Database Configuration (Optional - uses Supabase by default)
# PostgreSQL connection string if using external database
# DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# Supabase Service Role Key (Optional - for admin operations)
# Only needed for advanced admin features
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Set to true to enable SendGrid sandbox mode (emails won't actually send)
# Useful for testing email functionality without sending real emails
# SENDGRID_SANDBOX=true
